<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gymapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WorkoutDetail1Activity" />
        <activity android:name=".WorkoutDetail2Activity" />
        <activity android:name=".WorkoutDetail3Activity" />
        <activity android:name=".DietDetail1Activity" />
        <activity android:name=".DietDetail2Activity" />
        <activity android:name=".DietDetail3Activity" />
    </application>

</manifest>
